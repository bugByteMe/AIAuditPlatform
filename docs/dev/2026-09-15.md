# 2026-09-15

- Fixed Codex chat event parsing for `item.completed` events whose assistant text is nested at `item.text`; added a backend regression test for agent message rendering.
- Moved persistent account records into account storage and chat sessions/runs/events into dedicated chat storage; workspace metadata now keeps workspace state and lightweight session references only.
