# Development Record - 2026-09-12

## Changes

- Added root `AGENT.md` with short repo-specific design and coding rules.
- Established `docs/dev/<yyyy-mm-dd>.md` as the location for development records.
- Implemented a static frontend prototype in `index.html`, `styles.css`, and `app.js`.
- Added Chinese-default and English UI copy for workspace, live chat, artifacts, and admin workflows.
- Moved artifact download controls into workspace management, added workspace-owned chat sessions, compact runtime metadata, and collapsible left navigation.
- Removed chat sessions from workspace management so it shows artifacts only; added copy and delete controls to chat session rows.
- Workspace card selection now updates the artifact/download panel immediately; the Open action additionally navigates to the workspace chat sessions.
- Chat session rows now keep the main row as the session selector while right-side action buttons handle copy and delete.

## Notes

- Current design details live under `docs/`, with `docs/main.md` as the high-level entrypoint.
- The frontend currently uses mocked data and client-side interactions only; backend API integration remains future work.
- Copy and delete session actions are mock frontend operations and still need backend API design before production use.
