# 2026-09-18

- Exposed each signed-in user's exact remaining and total token budget in the sidebar and refresh it after a chat reaches a terminal state.
- Added click-to-edit workspace and chat session names. Workspace updates retain owner/system-admin authorization; chat titles persist through a new authenticated session PATCH endpoint.
- Reworked the active chat layout to use the viewport remainder below the application header, with bounded internal scrolling and responsive sizing.
- Added chat-title validation and frontend wiring regression coverage.
- Added owner-controlled exclusive workspace run locking, disabled by default, with confirmed cross-session shared-directory concurrency and continued exclusion of uploads and manual mutations until the last run exits.
- Preserved the visible chat-event position during live refreshes unless the reader is already following the bottom, and added focused backend/frontend regression coverage plus a safe workspace database migration.
