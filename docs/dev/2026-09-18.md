# 2026-09-18

- Exposed each signed-in user's exact remaining and total token budget in the sidebar and refresh it after a chat reaches a terminal state.
- Added click-to-edit workspace and chat session names. Workspace updates retain owner/system-admin authorization; chat titles persist through a new authenticated session PATCH endpoint.
- Reworked the active chat layout to use the viewport remainder below the application header, with bounded internal scrolling and responsive sizing.
- Added chat-title validation and frontend wiring regression coverage.
- Replaced browser-only chat copies with persisted native Codex forks using unique session IDs, credential-free conversation-state copies, and pre-run checkpoints for active-chat forks.
- Fixed forked-chat messages and inline title editing so they target only the selected branch.
- Reduced large-chat fork load by copying only the selected native Codex rollout, bulk-writing forked events in one linear pass, and allocating new event IDs from the file tail instead of rescanning the full transcript; also removed the prefilled demo credentials and credential hint from the login page.
- Replaced browser-only chat removal with an owner/system-admin persistent delete API that rejects active runs and cleans session, terminal-run, event, Codex-home, and stale fork-checkpoint state while allowing empty workspaces and preserving independent forks.
- Added OCRmyPDF plus Simplified and Traditional Chinese Tesseract language packs to the Codex runner image.
- Closed short-lived SQLite metadata connections explicitly so the unit suite can clean up temporary databases on Windows.
- Added owner-controlled exclusive workspace run locking, disabled by default, with confirmed cross-session shared-directory concurrency and continued exclusion of uploads and manual mutations until the last run exits.
- Preserved the visible chat-event position during live refreshes unless the reader is already following the bottom, and added focused backend/frontend regression coverage plus a safe workspace database migration.
