# Development Notes — 2026-09-23

- Changed the managed-budget percentage from a lifetime-usage ratio to a recharge-cycle ratio: current balance divided by the balance immediately after the latest recharge.
- Manual administrator additions and confirmed XLSX recharge credits now persist the resulting quota as `rechargeBaselineQuota` and reset the displayed percentage to 100%.
- Existing managed accounts establish a baseline from their first successful balance refresh when no saved recharge baseline exists.
- Added focused regression coverage for percentage calculation and baseline persistence across both recharge paths.
