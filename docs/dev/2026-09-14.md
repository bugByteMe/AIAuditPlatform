# 2026-09-14 Development Notes

- Added a local chat runtime scheduler with persisted chat sessions, runs, events, workspace locking, terminal checkpointing, artifact refresh, and fake-runner test coverage.
- Added a Docker runner image definition for containerized Codex execution. The image is not built or run on this machine because the local Docker environment is unavailable.
- Added workspace-scoped chat APIs for sessions, run start/stop, event listing, and SSE streaming.
- Wired the frontend composer and stop button to backend chat APIs, with SSE live updates and polling fallback.
- Added per-user Codex base URL/API key settings, generated session-scoped `CODEX_HOME` files, and native Codex resume for follow-up prompts.
- Added backend config loading from `config/ai_audit.json` plus environment overrides, including `AI_AUDIT_CODEX_ROOT` and `SKILL_PATH`.
- Added optional skill folder copying into session-scoped Codex homes, web-search event rendering, running dots in active chats, and folded multiline command output.
