# 2026-09-17

- Reworked the system-admin view into group sections with nested active and pending accounts, disk-usage meters, group creation/deletion, disk-limit controls, user deletion, and token-budget reset actions.
- Added account schema v3 with nullable group disk limits and backward migration from versioned and legacy account data.
- Added logical per-user and per-group workspace usage aggregation and quota enforcement for workspace creation, uploads/replacements, forks, and chat starts; post-run snapshots provide overshoot detection.
- Added lifecycle-safe cascading account/group deletion across workspaces, snapshots, chats, event logs, Codex homes, sessions, and unreferenced blobs, while retaining audit history and protecting the signed-in/last system administrator.
- Added Codex `turn.completed` token parsing with input/output/cached breakdown, persisted run/session/user totals, and duplicate-event protection.
- Added regression coverage for migration, quota behavior, token accounting, deletion protection/timeouts, resource cleanup, and frontend admin wiring.
- Added static compute-node configuration and an authenticated HTTPS worker agent that launches the existing per-run Codex Docker container against shared workspace storage.
- Replaced configured-pool execution with resource-aware remote scheduling, CPU/memory reservations, health exclusion, persisted event cursors, idempotent worker starts, and renewable leases that fence worker/control-plane loss without unsafe automatic retries. Empty node configuration continues to use local Docker.
- Added the system-admin compute-node health/resource panel and available/total CPU and memory meters beside chat status; reduced the global header to its compact action row and removed the large Audit Console heading.
- Added scheduler/config/frontend regression tests but did not execute any tests, builds, servers, or browser checks on this local machine, per request.
- Replaced the monolithic workspace `metadata.json` and duplicated snapshot manifests with normalized SQLite workspace/checkpoint/file-version/artifact metadata; retained only current plus one previous distinct version per path, including the last content of deleted paths.
- Added reference-aware blob reclamation for displaced history and ordinary workspace deletion, with prepared-upload blob verification/recreation to avoid collection races. Populated legacy workspace metadata now requires a fresh storage directory rather than an automatic migration.
- Added regression cases for independent per-file retention, deleted-file history, shared/exclusive blob collection, legacy-storage refusal, and prepared-upload blob recreation; these tests were not executed locally, as requested.
- Replaced memory-heavy multipart folder uploads with persisted, resumable upload sessions that validate manifests on the control plane, stream bounded chunks to reserved compute workers, finalize hashes/blobs/files on shared storage, and commit metadata only after worker processing succeeds.
- Added upload mutation leases, quota reservations, worker upload capacity reporting, phased frontend progress, reconnect-by-reselection support, and regression coverage for bounded reads, duplicate chunks, cancellation, limits, and workspace locking.
