# Development Record - 2026-09-13

## Changes

- Moved frontend implementation into `frontend/`.
- Added initial stdlib Python backend in `backend/server.py`.
- Implemented account-control APIs for login, logout, session lookup, account listing, account creation/update, and audit-log listing.
- Added cookie-based sessions with enabled-account and concurrent-session checks.
- Migrated the frontend to authenticate through the backend before showing the app shell.
- Connected the admin view to backend account and audit-log APIs.
- Redesigned the frontend toward a denser flat layout with lighter section separators and tighter row-based lists.
- Added collapsible chat session and workspace file panels; the file explorer is collapsed by default.
- Added mock workspace file trees in the frontend for the file explorer.
- Split the frontend JavaScript and CSS into smaller modules so no frontend source file exceeds 500 lines.
- Reworked the chat layout CSS to use explicit grid areas, keeping the session list, chat history, and file panel aligned in one desktop row.
- Added local workspace storage under `workspace_storage/` for test-mounted shared disk behavior.
- Implemented workspace creation, listing, settings updates, forks, file trees, snapshots, artifact diffs, and zip downloads in the backend.
- Wired the frontend workspace list, upload flow, fork action, file explorer, artifacts, and download controls to backend workspace APIs.
- Added stdlib backend tests for workspace storage, permissions, snapshots, diffs, forks, safe paths, and zip manifests.
- Added workspace deletion with backend authorization, audit logging, and frontend delete action.
- Replaced direct folder upload with a creation modal that accepts an explicit workspace name and staged multi-folder upload.
- Added draggable chat side-panel widths and default-open collapsible folders in the file explorer.
- Moved workspace row actions to icon-only open/fork/share/delete controls and removed redundant workspace-management/global share buttons.
- Added double-click file previews with authorized raw/metadata/rendered endpoints, text/image/PDF handling, and cached LibreOffice Office-to-PDF conversion.
- Reworked artifact downloads into a file-explorer-style tree, added scrollable file trees, restored workspace-card click to selection-only behavior, and added workspace-management file upload/download/delete controls.
- Added backend APIs and tests for uploading files into an existing workspace and deleting workspace files or folders with snapshot/artifact refresh.
- Fixed raw file download headers for non-ASCII filenames by using strict ASCII fallback names and UTF-8 `filename*` values.
- Consolidated workspace-page artifact/download and file-management UI into one bottom file explorer that shows all workspace files, supports folder/file selection, and carries upload/download/delete actions.

## Notes

- Workspace, file explorer, and artifact views load from the backend after authentication; workspace mock data has been removed.
- Duplicate workspace display names are allowed because storage paths are generated workspace IDs.
- Chat runtime and Codex container execution remain prototype-only and are not yet backed by worker APIs.
- Demo credentials are `chen.audit / audit123` and `li.review / review123`.
- The prototype backend uses in-memory users, sessions, and audit logs; workspace metadata persists in `workspace_storage/metadata.json`.
- The backend currently serves the frontend static files and API from the same origin.
