# 2026-09-20

- Added managed MicuAPI account provisioning with one external token named by each immutable local user ID, startup reconciliation, and lifecycle cleanup.
- Replaced local token-count budget enforcement with fresh MicuAPI balance checks for managed mode while retaining cumulative token usage for reporting.
- Changed the admin budget workflow to add a CNY amount to the existing MicuAPI balance without resetting used tokens.
- Added managed/default and custom-provider choices to Codex settings; custom mode uses the user's own URL and key without Micu balance gating.
- Updated account persistence, public/admin budget summaries, configuration documentation, and focused budget/provisioning test expectations.
- Added an authenticated Recharge panel that exposes only the current user's managed MicuAPI URL/key, provides CNY 50/100/200 payment choices, and displays pre-deployed QR assets with a required username-note reminder.
- Documented manual payment reconciliation: QR display does not itself credit balance; administrators add the confirmed amount to the existing MicuAPI balance.
- Improved MicuAPI HTTP failures to report the response status and normalized media type while suppressing non-JSON response bodies; JSON provider messages are whitespace-normalized and length-limited.
- Added an explicit `AI-AuditPlatform/1.0` User-Agent to MicuAPI requests because the provider rejects Python urllib's default User-Agent with HTTP 403.
- Changed new-account provisioning and missing-binding reconciliation to name MicuAPI tokens with the local username instead of the generated user ID.
- Restricted user-facing budget data to a remaining percentage derived from MicuAPI quota, while retaining exact CNY balances in administrator-only account responses and keeping token usage visible.
- Extended startup reconciliation to synchronize persisted `micu.tokenName` values and rename legacy ID-named provider tokens to usernames when conflict-free.
- Limited initial workspace file-tree payloads to three levels, added permission-checked direct-child loading for deeper folder expansion, and preserved folder-level selection for downloads and deletion without eagerly transferring descendants.
- Reworded the user budget summary to combine remaining percentage with compact cumulative platform Token usage, and added focused backend/frontend regression coverage for both changes.
- Kept folder-before-file ordering stable at every tree level after lazy-loaded children are merged, preventing expanded folders from moving below sibling files.
