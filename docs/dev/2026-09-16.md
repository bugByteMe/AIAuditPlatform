# 2026-09-16

- Fixed chat viewport sizing to avoid full-page scroll, compacted command execution rows, made workspace creation uploads cancellable, disabled create while uploading, and removed workspace setup/fork pseudo-sessions from chat lists.
- Updated the Codex runner image to use Python 3.12 and include document/PDF/XLSX/OCR processing dependencies plus LibreOffice, Poppler, Pandoc, and Tesseract; split install layers, added configurable pip mirror args for reliable ECS builds, and repaired npm/npx symlinks after the multi-stage Node copy.
- Restored the accidentally truncated frontend localization and rendering modules, made JavaScript MIME types deterministic for module loading on Windows, and added frontend asset smoke tests.
- Added active-chat thinking animation and one-line expandable command events; implemented versioned account/group storage, system-admin batch invitations, invite revocation, invite registration with automatic login, and related backend/frontend regression tests.
- Made container worktree permission preparation portable to Windows test environments where `os.chown` is unavailable.
- Hardened chat history live updates with session-targeted event merging, monotonic cursors, stale-subscription guards, proxy-safe SSE headers, terminal-state reconciliation, and an always-on polling watchdog.
- Completed runtime configuration coverage for service, upload, preview, SSE, scheduler, container shutdown, audit retention, and account-policy settings; added browser-safe runtime configuration delivery.
